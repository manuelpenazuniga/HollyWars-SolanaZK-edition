// Attestor service: GitHub OAuth + account checks, Proof of Passion scoring,
// Ed25519 attestations, and off-chain Merkle census maintenance.
// Knows identities; never sees votes. Runs as a separate process from the relayer.
export {};
